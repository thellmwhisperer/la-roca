schema: 1
id: R5
status: removed
category: adjacent_feature
title: "S1 retained playground boundary residue"
created_at: 2026-09-09
incident:
  intent: "Correct what the cleanup wave added, dismantle nothing else."
  what_was_built: "internal/provider/query/prompt.go still owns prompt/schema helpers after the answering implementation moved to the optional playground executable. Core retains CLI and MCP adapters and plugin installation metadata."
  justifying_sentence: "Accepted for the separate S1 boundary completion requested by issue #385. The existing S1 removal forbids stay enforced. Do not move answering-model inference back into core or broaden this reader correction into a provider redesign."
  source: "https://github.com/thellmwhisperer/la-roca/issues/450, residue R5; issue #385"
  cost: "The retained prompt file is about 500 lines; byte and timing costs of the remaining boundary are not measured."
rule: "Schema types, descriptions and the SQL gate stay in core. Prompt builders, refusal and referent detection, and the lexical rescue belong to the optional playground plugin. Product retrieval uses Service.Search."
forbid:
  paths:
    - "internal/provider/query/refusal.go"
    - "internal/provider/query/referent.go"
    - "internal/provider/service/lexical.go"
  symbols:
    - "SQLSystemPrompt"
    - "SQLUserPrompt"
    - "IsRefusal"
    - "DetectMissingReferent"
    - "SubstringLikeRejection"
    - "TemplateSearchByTerm"
    - "SearchByTerm"
  strings:
    - "You are an expert SQL assistant"
